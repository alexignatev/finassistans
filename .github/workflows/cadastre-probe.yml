name: Cadastre multipart probe

on:
  pull_request:
    branches: ["main"]
    paths:
      - "cadastre_pilot/**"
      - ".github/workflows/cadastre-probe.yml"
  workflow_dispatch:

concurrency:
  group: cadastre-probe-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install geospatial image dependencies
        run: python -m pip install --upgrade requests numpy opencv-python-headless pyproj

      - name: Query legacy PKK and reconstruct parcel contours from selected raster
        run: python cadastre_pilot/legacy_probe.py 2>&1 | tee cadastre_probe.log

      - name: Preserve logs
        if: always()
        run: |
          mkdir -p cadastre_result
          cp cadastre_probe.log cadastre_result/run.log || true

      - name: Upload probe result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cadastre-probe
          path: cadastre_result
          if-no-files-found: warn
          retention-days: 7
